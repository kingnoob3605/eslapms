import { createAction } from '@ngrx/store';

export const expandSidebar = createAction('[Sidebar] expand sidebar');
export const collapseSidebar = createAction('[Sidebar] collapse sidebar');
export const hideSidebar = createAction('[Sidebar] hide sidebar');
